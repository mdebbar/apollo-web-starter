{
  "name": "web",
  "version": "0.0.1",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "TODO: BUILD && UPLOAD TO S3 && UPDATE WEB SERVER TO POINT TO NEW ASSETS"
  },
  "dependencies": {
    "apollo-client": "^0.7.3",
    "auth0-lock": "^10.9.2",
    "graphql-tag": "^1.2.3",
    "jwt-decode": "^2.1.0",
    "react": "^15.4.2",
    "react-apollo": "^0.8.1",
    "react-dom": "^15.4.2",
    "react-router": "^3.0.1"
  },
  "devDependencies": {
    "react-scripts": "0.8.5"
  }
}
